api_id = 0 # here your api_id
api_hash = '0' # here your api_hash

strict_filter = ['TEST'] # strict filters
lax_filter = ['te', 'st'] # lax filters in lowercase

usernames = ['fisrt_username', 'second_username'] # username the script to work with chats
username_exception = ['my_username'] # username the script not to work with chats (often your username)
